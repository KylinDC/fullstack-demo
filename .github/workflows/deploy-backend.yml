name: Deploy Backend

on:
  push:
    branches:
      - main
    paths:
      - 'apps/backend/**'
      - '.github/workflows/deploy-backend.yml'
  workflow_dispatch:

jobs:
  test:
    name: Test Backend
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '22'

      - name: Install pnpm
        uses: pnpm/action-setup@v4

      - name: Get pnpm store directory
        shell: bash
        run: |
          echo "STORE_PATH=$(pnpm store path --silent)" >> $GITHUB_ENV

      - name: Setup pnpm cache
        uses: actions/cache@v4
        with:
          path: ${{ env.STORE_PATH }}
          key: ${{ runner.os }}-pnpm-store-${{ hashFiles('**/pnpm-lock.yaml') }}
          restore-keys: |
            ${{ runner.os }}-pnpm-store-

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Run lint
        run: pnpm --filter backend lint

      - name: Run tests
        run: pnpm --filter backend test

      - name: Build
        run: pnpm --filter backend build

  deploy:
    name: Deploy to Production
    runs-on: ubuntu-latest
    needs: test

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}

      - name: Build and push Docker image
        uses: docker/build-push-action@v5
        with:
          context: .
          file: apps/backend/Dockerfile
          push: true
          tags: |
            ${{ secrets.DOCKER_USERNAME }}/fullstack-demo-backend:latest
            ${{ secrets.DOCKER_USERNAME }}/fullstack-demo-backend:${{ github.sha }}
          cache-from: type=gha
          cache-to: type=gha,mode=max

      # Example deployment to a cloud provider
      # Uncomment and configure based on your deployment target

      # - name: Deploy to Railway
      #   run: |
      #     curl -X POST ${{ secrets.RAILWAY_WEBHOOK_URL }}

      # - name: Deploy to AWS ECS
      #   uses: aws-actions/amazon-ecs-deploy-task-definition@v1
      #   with:
      #     task-definition: task-definition.json
      #     service: backend-service
      #     cluster: production-cluster

      # - name: Deploy to Google Cloud Run
      #   uses: google-github-actions/deploy-cloudrun@v2
      #   with:
      #     service: backend
      #     image: ${{ secrets.DOCKER_USERNAME }}/fullstack-demo-backend:${{ github.sha }}
      #     region: us-central1
